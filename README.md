# calibrator
The Calibrator, a Cache-Memory and TLB Calibration Tool.

## Environment
Linux kernel version >= 4.11.0  
CMake version >= 3.5.2

## Usage
```
mkdir build
cd build
cmake ..
make
cd ../bin
./calibrator
```